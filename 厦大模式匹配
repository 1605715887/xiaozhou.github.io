#include <stdio.h>
#include<string.h>
int main()
{
	int N,count=0;
	char t[21]={0};
	char m[11]={0};
	scanf("%d",&N);
	scanf("%s",t);
	while(N--)
	{
		scanf("%s",m);
		if(strstr(t,m)!=NULL)
		count++;
	}
	printf("%d",count);
	return 0; 
}
