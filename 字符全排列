#include<iostream>
#include<algorithm>
#include<cstring>
using namespace std;
int main()
{
	string line;
	cin >> line;
	sort(line.begin(), line.end());
	cout << line << endl;
	while (next_permutation(line.begin(), line.end()))
		cout << line << endl;
	return 0;
}
